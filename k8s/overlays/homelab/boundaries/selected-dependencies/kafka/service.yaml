apiVersion: v1
kind: Service
metadata:
  name: kafka-main
  namespace: faang
  labels:
    app.kubernetes.io/part-of: faang-dependencies
    faang.io/dependency: kafka
    faang.io/dependency-mode: external
  annotations:
    argocd.argoproj.io/sync-wave: "-49"
spec:
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
      protocol: TCP
