apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Retained local backup store. It is deliberately not a future runtime S3
# profile boundary and has no prospective Argo Application in DEP-051.
namespace: faang
resources:
  - s3.yaml
