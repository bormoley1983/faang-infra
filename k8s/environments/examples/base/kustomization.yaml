apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: faang

resources:
  - ../../../base

patches:
  - path: dependency-endpoints.yaml
    target:
      kind: ConfigMap
      name: faang-config
