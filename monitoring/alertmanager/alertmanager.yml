route:
  receiver: 'null'
  group_by: [alertname, cluster, service]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: 'null'

# Add real receivers only in the private operations source. Webhook URLs,
# SMTP credentials, tokens, and private destinations must not be committed.
