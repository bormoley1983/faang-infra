pipeline {
    agent any
    stages {
        // ... (Build and Push stages same as before)

        stage('Deploy') {
            steps {
                // Kustomize is built into kubectl. Just point to the overlay.
                sh "kubectl apply -k faang-infra/k8s/overlays/homelab"
            }
        }
    }
}
